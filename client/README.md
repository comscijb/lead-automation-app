# Lead Scoring App

Portfolio-ready React interface for creating leads, scoring sales priority, and tracking pipeline status.

## Scripts

```bash
npm run dev
npm test
npm run build
```

The Vite dev server runs on [http://localhost:5000](http://localhost:5000) by default. Configure the API URL with `VITE_API_URL` in `.env`.
